[project]
name = "manga-explainer"
version = "0.1.0"
description = "Low-RAM manga explanation video pipeline (skeleton)"
requires-python = ">=3.9"
dependencies = ["PyYAML>=6.0"]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]